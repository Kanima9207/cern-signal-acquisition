# Firmware

This directory is reserved for the embedded acquisition layer.

Planned hardware path:

```text
Sensors / signal source -> analog conditioning -> ADC -> STM32 -> USB/serial -> PC DSP pipeline
```

Hardware implementation will be added after laboratory access and component clearance are available.
